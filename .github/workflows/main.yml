name: Waka Readme

on:
  workflow_dispatch:
  schedule:
    # Runs daily at 00:00 UTC.
    - cron: "0 0 * * *"

permissions:
  contents: write

concurrency:
  group: waka-readme
  cancel-in-progress: true

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Update WakaTime statistics
        uses: athul/waka-readme@8539bedb8277066fc9fe1b47d1f9b108eb866494 # v0.3.1
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_TITLE: true
          BLOCKS: ->
          TIME_RANGE: last_7_days
          LANG_COUNT: 6
          SHOW_TIME: true
          SHOW_MASKED_TIME: true
          COMMIT_MESSAGE: "chore: update weekly coding activity"
